# Local Android libraries

Copy the generated sing-box Android libbox artifact here as:

```text
libbox.aar
```

The AAR itself is ignored by Git. When present, `app/build.gradle.kts` links it automatically.
